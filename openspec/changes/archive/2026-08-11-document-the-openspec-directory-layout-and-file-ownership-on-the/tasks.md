# Tasks

- [x] **T001** [claims:openspec-tree-documented] The artifacts page documents the `openspec/` tree itself — every top-level file and directory, which of them an install overwrites, where `foundation.json` lives and why, and that the project-level and packet-level `repositories.yaml` are different files — in English and Thai — verify: the documentation suite asserts each entry and the ownership statement, and the docs site builds [repo:root] [paths:website/docs/src/content/docs/**,.claude/tests/docs/**]
