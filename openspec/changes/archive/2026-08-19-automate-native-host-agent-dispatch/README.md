# automate-native-host-agent-dispatch

Add a resumable host-facing dispatcher contract that turns the existing execution graph into bounded native-agent spawn decisions without making the harness invoke models.
