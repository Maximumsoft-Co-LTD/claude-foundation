# Tasks

> This is the sole implementation ledger. Check an item only when its verify
> condition passes. Group by coherent behavior, not workflow phase. Add
> `[claims:<claim-id>]` after each stable task ID once evidence claims exist.

- [x] **T001** Parse and validate `sandbox.setupCommand` / `sandbox.setupTimeoutMs` in `foundationPolicy()` — `.claude/harness/runtime/core/runtime-environment.mjs` — verify: invalid types fail with the field name; valid config surfaces on the policy object [repo:root] [paths:.claude/harness/runtime/core/runtime-environment.mjs] [claims:setup-config-validation]
- [x] **T002** Run the configured setup command after single-repository sandbox creation (worktree and copy modes), record `workspace.setup`, warn and keep the sandbox on failure — `.claude/harness/runtime/workflow/sandbox-runtime.mjs` — verify: contract tests cover ok / absent / failed [repo:root] [paths:.claude/harness/runtime/workflow/sandbox-runtime.mjs] [claims:setup-command-runs,setup-absent-noop,setup-failure-preserves-sandbox] [depends:T001]
- [x] **T003** Parse per-repository `setupCommand` from `openspec/repositories.yaml` and run it per repository sandbox in multi-repository creation — `.claude/harness/runtime/workflow/repository-topology.mjs`, `.claude/harness/runtime/workflow/sandbox-runtime.mjs` — verify: multi-repository contract test sees per-repo setup outcome [repo:root] [paths:.claude/harness/runtime/workflow/repository-topology.mjs] [claims:setup-multi-repo] [depends:T002]
- [x] **T004** Supply the new factory inputs at the composition root — `.claude/harness/foundation.mjs` — verify: `run-wiring-tests.sh` passes [repo:root] [paths:.claude/harness/foundation.mjs] [claims:setup-wiring] [depends:T002]
- [x] **T005** Contract tests: single-repo setup ok / absent / failed in `sandbox-land.sh`; per-repo setup in `multi-repository.sh` — verify: `run-harness-tests.sh` green including new assertions [repo:root] [paths:.claude/tests/harness/contracts/sandbox-land.sh,.claude/tests/harness/contracts/multi-repository.sh] [claims:setup-command-runs,setup-absent-noop,setup-failure-preserves-sandbox,setup-multi-repo] [depends:T002,T003]
- [x] **T006** Evidence view: TAP wrapper restating `run-harness-tests.sh` + `run-wiring-tests.sh`, plus README row — `.claude/tests/harness/run-sandbox-seam-tap.sh`, `.claude/tests/README.md` — verify: wrapper emits `1..N` with N above the configured floor [repo:root] [paths:.claude/tests/harness/run-sandbox-seam-tap.sh,.claude/tests/README.md] [claims:setup-command-runs] [depends:T005]
- [x] **T007** Document `sandbox.setupCommand` and the repositories.yaml field in the runtime reference — `.claude/harness/README.md` — verify: doc names both config homes and the failure semantics [repo:root] [paths:.claude/harness/README.md] [depends:T002,T003]
