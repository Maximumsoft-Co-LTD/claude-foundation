{
  "version": 2,
  "claims": [
    {
      "id": "multi-repository-replay",
      "scenario": "When one or more writable repository targets move, sandbox sync replays every changed repository and advances all recorded bases; if any replay conflicts, every live sandbox and recorded base remains unchanged and the repository-qualified conflict is reported",
      "impact": "high",
      "capabilities": ["test", "review"]
    },
    {
      "id": "content-stable-evidence",
      "scenario": "Changing only recorded repository base commit identities does not change composite workspace or code hashes, while a tracked content change still changes them and unreconciled target movement still blocks Land",
      "impact": "high",
      "capabilities": ["test", "compatibility", "review"]
    }
  ]
}
