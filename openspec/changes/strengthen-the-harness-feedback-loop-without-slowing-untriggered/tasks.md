# Tasks

> This is the sole implementation ledger. Check an item only when its verify
> condition passes. Group by coherent behavior, not workflow phase.

- [x] **T001** Parse unattended intent monotonically before telemetry and reject malformed or duplicate security flags before any mutation — runtime and CLI routing — verify: direct-runtime and public-CLI negative matrix with unchanged state/log/worktree digests
- [x] **T002** Make isolation inspect and unattended preflight launch zero child processes while preserving interactive behavior — filesystem-only workspace diagnostics — verify: fake-PATH sentinels and macOS/Linux fixtures
- [x] **T003** Reject every canonical, traversal, file-URI, and symlink-resolved prototype origin from receipts and proof atomically — artifact/reference validators — verify: no receipt or evidence-vault mutation plus positive controls
- [x] **T004** Make `/prototype` always persist a decision and return an explicit `/change --prototype-selection <path>` handoff — command contracts and installer surface — verify: path grammar, no auto-discovery, byte-identical installs, and command budgets
- [x] **T005** Union committed and dirty paths from the recorded base for root, child, worktree, and copy repositories — canonical changed-surface primitive — verify: commit/stage/untracked/rename/delete and missing-base fixtures
- [x] **T006** Derive review requiredness and diversity from one assurance policy covering critical capabilities, semantics, and multi-repository writes — validate/provider/packet/fingerprint consumers — verify: risk matrix and ordinary Rapid control
- [x] **T007** Replace parallel provenance arrays with bounded reviewer/subject tuples and recompute independence/diversity on every read — review protocol and CLI grammar — verify: multi-subject correlation, tamper, same-session, same-family, and human cases
- [x] **T008** Persist a change-level monotonic, hash-chained review-attempt history independent of current receipt/provider identity — evidence vault and runtime state — verify: receipt deletion/provider rename cannot reset two-AI limit; corrupt history fails closed; human re-review remains available
- [x] **T009** Resolve acceptance to explicit valid claim IDs and fully revalidate human identity, criteria, observation, provenance, reason, workspace, claims, and artifact/reference — acceptance protocol — verify: field-by-field tamper, legacy-all migration, and post-edit staleness
- [x] **T010** Reject non-external review/acceptance adapters and emit executable AI/human readiness grammar without placeholders — execution validation, public CLI, help, and docs — verify: configuration failures and copy-run templates
- [x] **T011** Keep scoped protocol migration, installer contents, English/Thai documentation, and immutable proof trust aligned — protocol bundle, upgrade tests, and proof audit — verify: targeted stale reasons and backward-compatible unrelated receipts
- [x] **T012** Demonstrate speed and quality release gates — benchmark fixtures/rationale — verify: untriggered +0 requests/spawns/artifacts, review packet <=8 KiB, median/p95 ratchet, adversarial false-acceptance zero, full suite, and diff consistency
