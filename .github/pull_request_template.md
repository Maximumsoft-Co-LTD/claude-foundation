## What & why

<!-- What changed and what problem it solves. Link the issue if one exists. -->

## Shipped surface

<!-- Does this touch files installed into consumer projects
     (.claude/**, openspec/schemas/**, WORKFLOW.md, install*.sh)?
     If yes: which lane (Runtime / Instruction / Shipping), and are the
     matching deterministic tests / protocol pins / commands.json updated? -->

## Evidence

- [ ] `sh .claude/tests/run-all.sh` is green locally
- [ ] Commits are signed off (`git commit -s`, DCO)
- [ ] Diff contains only changes traceable to this request
