---
/**
 * Deliberately empty. The docs render in one light theme to match the landing
 * page, so Starlight's light/dark/auto selector has nothing to switch between.
 *
 * The colour variables are pinned in ../styles/brand.css for every value of
 * `data-theme`, so this only removes the control — it is not what enforces the
 * theme, and deleting this file would not resurrect dark mode.
 */
---
