# Foundation runtime

This directory is machine-owned. Runtime state, receipts, provider logs, and
sandboxes are intentionally ignored. Durable intent belongs in `openspec/`;
implementation truth belongs in code and tests.
