# Orchestrator reference — Fast path (XS/S/M)

> Loaded on demand by the main agent (`.claude/orchestrator.md`). The `## Size-aware execution` section keeps the size picker, **Never shrinks**, **Patch lane (XS subtype)**, and the size/field upgrade rules inline; this holds the fast-path deltas. Read it when `size` ∈ {XS, S, M}. Everything not listed here runs as written in the main flow; **M-scoped deltas are only where marked M** — the rest of this file is XS/S.

- **One question batch (XS/S).** Fold setup + interview into one `AskUserQuestion` (≤4, catch-all last). (M keeps the split setup + interview batches — size matrix.) Over 4 → drop the type question (record as `Assumptions (inferred)`), then branch (propose in intro); never drop an interview slot or the catch-all. Checkout right after. **Say the path** in the status line + gate summary (e.g. "Size: XS → fast path: combined spec+plan, merged docs+ship, inline retro").
- **Spec+plan+tasks+test-plan in one spawn (XS/S/M).** Skip `pm`; spawn `lead` combined mode (`.claude/agents/references/lead.md > Combined variant`) with digest + Q&A; the spec/plan/tasks/test-plan checks run on the returned artifacts. **M:** model **opus** (spec quality rides the plan spawn); brownfield M builds `context.md` FIRST (Design's Context sub-step — integration points from the digest) and passes it; plan sections at full Mode A depth per plan `Size`. XS/S: sonnet, compact sections. **Clean-tree check after return:** `git -C <repo_root> status --porcelain` — any source change means lead implemented early; revert, note, proceed. Test plan folds in (feat/fix/refactor; gate surfaces it, pre-gate scan still checks each AC has a Coverage-plan row; chore/docs/spike → append `test-plan` to `skipped_steps`). Handle return first line (`SIZE_UPGRADE`/`FIELD_UPGRADE`/`BLOCKER`).
- **XS micro-lane artifact: one `run.md`.** At `size=XS` the combined `lead` spawn writes `.workflow/<id>/run.md` (`_templates/run.md`) INSTEAD of the four files — same contract core (Goal · `**Type**:` · `AC#` · `T###`+`verify:` · Coverage lines), one artifact. The artifact checks + pre-gate scan run against `run.md`; state cursor names are unchanged (the combined write still records `step=plan|test-plan`, `next_step=gate`); Test-inline reads its `## Coverage` Impacted cmd. `SIZE_UPGRADE: S` → the re-spawn emits the full four-artifact set (`run.md` stands as digest input; delete it after the fold). S and M keep the four files.
- **Test inline** (feat/fix/refactor, `e2e_visual=off`): the engineer implemented the planned tests (engineer Mode A rule); run the **Impacted** cmd from `test-plan.md > Commands` via Bash yourself and write `tests.md` from the template — one row per Coverage-plan AC + result, Commands, (fix) the pre-fix-fail evidence from the engineer's two-commit sequence. Failing → route to `engineer` (`cycles.test++`, limit 3, unchanged). `e2e_visual=on` → spawn `qa` as usual.
- **Review stays; fanout XS/S-scoped** — `lead` attempting to direct-nest review helpers on XS/S is itself evidence of a size-upgrade; return `SIZE_UPGRADE:` instead. (M: the size matrix governs — core 3-lens review fanout allowed when justified.) **Docs+ship in one spawn** (Mode B then C); Implement stays its own spawn.
- **Skipped-test stub inline** for chore/docs (write `tests.md > Skipped` yourself). **Retro: XS/S inline** (stamp `done_at` first, write `retro.md` from template yourself + update INDEX/FOLLOWUPS; skill round only if something surfaced).
