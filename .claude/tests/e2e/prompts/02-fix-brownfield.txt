/dev --yes fix: admin login loops back to the login page after a valid sign-in instead of landing on the dashboard. Size S, brownfield. Root-cause hypothesis: the session cookie is written after the redirect decision reads it. Acceptance: (AC1) a valid admin sign-in lands on the dashboard and is not bounced back to login. Do not ask clarifying questions — assume: Ship as one-drop, commit yes, no PR, e2e off. The first task must be a failing regression test that reproduces the loop, then the fix. Move straight through the interview and gate, then implement.
