/dev --yes create a single-page task list app. Size S, greenfield. A user adds items, marks them complete, and the list persists in localStorage across reloads. Acceptance: (AC1) adding "buy milk" to an empty list shows it as an open item; (AC2) toggling an item persists its completed state across a reload. Do not ask clarifying questions — assume: Ship as one-drop, no commit, no PR, e2e off. Use these answers to move straight through the interview and gate, then implement.
