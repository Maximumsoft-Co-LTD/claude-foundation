/dev --yes now the same address book needs the results ordered — people want the list to come back alphabetically by name instead of in whatever order it is stored (type feat, size XS, brownfield). Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement.
