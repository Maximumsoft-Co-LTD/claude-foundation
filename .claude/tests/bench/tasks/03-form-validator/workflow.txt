/dev --yes create a signup form validator function (size S, greenfield). validate(form) checks fields and returns { valid, errors }. Requirements — AC1: valid:true with empty errors only when all fields pass; AC2: email must match a basic name@domain.tld shape else errors.email; AC3: password must be >= 8 chars else errors.password; AC4: password must equal confirmPassword else errors.confirm; AC5: any empty required field (name, email, password) sets an error naming it. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement with unit tests covering each rule and its edge case.
