AC1: A function serializes an array of row objects to CSV text — one header line from the keys, then one line per row.
AC2: A field value containing a comma is wrapped in double quotes.
AC3: A field value containing a double quote has that quote escaped by doubling it, and the field is quoted.
