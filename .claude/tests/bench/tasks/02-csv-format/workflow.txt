/dev --yes create a CSV serializer function (size XS-S, greenfield). Requirements — AC1: serialize an array of row objects to CSV text, one header line from the keys then one line per row; AC2: a value containing a comma is wrapped in double quotes; AC3: a value containing a double quote has the quote escaped by doubling it and the field quoted. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement with unit tests.
