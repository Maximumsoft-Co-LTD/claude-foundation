Write a CSV serializer function in this repository. Requirements — AC1: serialize an array of row objects to CSV text, one header line from the keys then one line per row; AC2: a value containing a comma is wrapped in double quotes; AC3: a value containing a double quote has the quote escaped by doubling it and the field quoted. Write the function and unit tests directly. Do not use any workflow, do not create planning or spec files — just implement it.
