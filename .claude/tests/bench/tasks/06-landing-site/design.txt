/dev --yes --plan-only build a landing website for claude-foundation from PRODUCT-BRIEF.md in this repo (size S, greenfield, UI). Requirements — AC1: self-contained, no network requests (no CDN script/stylesheet, no remote font or image; CSS/JS local or inline, art as inline SVG or data URI); AC2: content from the brief — product name plus a one-line value proposition above the fold, a features section naming the /dev pipeline, sub-agents, hooks and skills, and a getting-started section with the real install command and how to invoke /dev, no placeholder copy; AC3: responsive at 375px and 1280px with no horizontal overflow; AC4: accessible — semantic landmarks, exactly one h1, text alternatives for non-decorative graphics, visible keyboard focus styles; AC5: dark mode via prefers-color-scheme with adequate contrast in both themes. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate. Stop after the gate — do not implement.
