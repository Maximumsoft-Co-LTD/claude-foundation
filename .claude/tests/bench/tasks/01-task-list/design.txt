/dev --yes --plan-only create a single-page task list app (size S, greenfield). Requirements — AC1: adding "buy milk" to an empty list shows it as an open item; AC2: toggling an item persists its completed state across a page reload via localStorage; AC3: an empty title is rejected. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement and write the tests.
