/dev --yes create a pagination helper function (size S, greenfield). paginate(items, page, size) returns a page of a list plus metadata. Requirements — AC1: correct slice for a normal in-range page, 1-indexed; AC2: a page beyond the last returns empty items, not an error; AC3: a size larger than total returns all items on page 1; AC4: empty items returns empty with totalPages 0; AC5: result includes page, size, total, totalPages. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement with unit tests covering each boundary.
