# Contact panel state defect

The Contacts button opens the panel but cannot close it. Preserve the query and
selection while toggling, keep the React component controlled, and add a
regression test that fails against the original reducer.
