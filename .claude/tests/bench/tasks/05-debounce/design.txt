/dev --yes --plan-only create a debounce utility (size S, greenfield). debounce(fn, ms) returns a debounced function. Requirements — AC1: fn fires only ms after the last call (trailing edge); AC2: rapid calls within the window coalesce into one fn call with the latest arguments; AC3: the returned function has cancel() that prevents a pending call; AC4: calls more than ms apart each fire once. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement with unit tests (use fake timers) covering coalescing and cancel.
