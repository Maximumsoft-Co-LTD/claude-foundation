/dev --yes --plan-only bug #412 in this repo — a workspace set its activity-strip window to 0 to hide the strip and got its entire event history back instead of nothing (type fix, size XS, brownfield). Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate. Stop after the gate — do not implement.
