/dev --yes --plan-only accounting flagged order #1183 in this repo — the invoice prints a total one cent higher than the line amounts add up to, and they re-key it by hand every time (type fix, size S, brownfield). Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate. Stop after the gate — do not implement.
