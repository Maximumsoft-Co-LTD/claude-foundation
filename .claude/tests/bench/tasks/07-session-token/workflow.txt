/dev --yes create a session-token module for our web app (size S, greenfield). issue(userId) gives me a token I can hand to a browser, and verify(token) tells me whether it is still good and which user it belongs to. Sessions should last 15 minutes. Do not ask clarifying questions — assume Ship as one-drop, no commit, no PR, e2e off. Move straight through the interview and gate, then implement with unit tests.
