# Address book

The team's contact list. `contacts.js` owns the data; `render.js` draws the
address-book screen and the CSV exporter also reads `listContacts()`.

Support keeps hearing the same thing: the list has grown and people scroll
forever looking for someone.
